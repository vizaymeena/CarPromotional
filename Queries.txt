1. cloneNode(false) : copies element itself                     // shallow copy
2. cloneNode(True) : copies element and its childer too        // Deep copy
3. .appendChild(object.assign())
4. removeAttribute()
5. what is node list and why event listerner does not work directly on them


Or don’t let the text expand at all on hover — just show tooltip or use a modal/pop-up for full text.